[package]
name = "delta_ttk"
version = "0.1.0"
edition = "2024"

[dependencies]
fltk = "^1.5"

# Mágica para deixar o executável incrivelmente pequeno:
[profile.release]
opt-level = 'z'
lto = true
strip = true
codegen-units = 1
panic = "abort"